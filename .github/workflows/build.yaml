
name: Build Web App

on:
    pull_request:
        branches:
            - main

jobs:
    build: 
        runs-on: ubuntu-18.04
    
        steps:
        - uses: action/checkout@v2

        - name: Docker Build
          run: docker build -t app ./src
          


